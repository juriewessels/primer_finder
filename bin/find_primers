#!/usr/bin/env ruby

require_relative '../lib/find_primers.rb'

logger = Logger.new(STDOUT)
logger.info("Finding primers...")

vendors = JSON.parse(File.read('./vendors.json'), symbolize_names: true)
results = FindPrimers.call(vendors: vendors)

logger.info(results)